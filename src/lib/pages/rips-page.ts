import { html } from "htm/preact"
import styled from "styled-components"
import { RipsIndex } from "../modules/rip/rip-index";

export const RipsPage = ({ id }) => {

    return html`<${RipsIndex}></${RipsIndex}>`
}